<nav class="navbar navbar-default">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/">Event App</a>
        </div>
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                <li><a href="/home">Home</a></li>
                <li><a href="/logout">Log Out</a></li>
            </ul>
            <span class="welcomeMessage"> WELCOME {{first}} {{last}}! ID:</span><span id="user_id">{{user_id}}</span>
        </div>
    </div>
</nav>

<div class="homeContainer">
    <div class="eventDisplayInfo">
      
        <h1>{{event.event_name}}</h1> 
        <h3>Location: {{event.location}}</h3>
        <h3>Date: {{event.event_date}}</h3>
        <h3>Time: {{event.start_time}} - {{event.end_time}}</h3>


        {{!-- <ul>
        {{#each guests}}
            <h3>{{guest_name}} : {{item_name}}</h3>
        {{/each}}
        </ul> --}}

    </div>
    
    <a href="/home"><button class="btn btn-lg home">Home</button></a>

</div>








